#include <stdio.h>

/**
 * Main - Entry point
 * Return: 0 When success
 */
int main(void)
{
    char str[] = "\"Programming is like building a multilingual puzzle \n";
    puts(str);
    return (0);
}
